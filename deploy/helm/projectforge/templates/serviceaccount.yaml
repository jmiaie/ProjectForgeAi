{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "projectforge.serviceAccountName" . }}
  labels:
    {{- include "projectforge.labels" . | nindent 4 }}
{{- end }}
